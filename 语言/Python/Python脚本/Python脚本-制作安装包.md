```python
from setuptools import find_packages
from setuptools import setup
version="1.0.1"
setup(
    name
    version
    author
    url
    description
    packages=(exclude=("docs"))
    packages_data={"mindvision.config":['*']}
    include_package_data=True
)
```

